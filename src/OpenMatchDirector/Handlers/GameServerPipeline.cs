namespace OpenMatchDirector.Handlers;

/*
public sealed class GameServerPipeline
{
    private List<IPipelineStep> _steps = [];

    public void AddStep(IPipelineStep step) => _steps.Add(step);

    public void Execute()
    {
        foreach (var step in _steps)
        {
            step.Execute();
        }
    }
}
*/